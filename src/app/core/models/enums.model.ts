export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN'
}

export enum TokenType {
  AUTH = 'AUTH',
  ACCESS = 'ACCESS',
  REFRESH = 'REFRESH'
}

export enum NotificationType {
  LIKE = 'LIKE',
  COMMENT = 'COMMENT',
  FOLLOW = 'FOLLOW'
}
